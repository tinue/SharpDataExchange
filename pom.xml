<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>ch.erzberger</groupId>
    <artifactId>SharpDataExchange</artifactId>
    <version>0.1.0</version>

    <name>SharpDataExchange</name>
    <description>Data exchange between a modern PC and Sharp Pocket Computers via USB/Serial</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <version.jSerialComm>2.11.4</version.jSerialComm>
        <version.lombok>1.18.42</version.lombok>
        <version.commons-cli>1.11.0</version.commons-cli>
        <version.sharpbasic>1.0.0-SNAPSHOT</version.sharpbasic>
        <version.junit>6.0.3</version.junit>
        <version.maven-compiler-plugin>3.15.0</version.maven-compiler-plugin>
        <version.maven-shade-plugin>3.6.2</version.maven-shade-plugin>
        <version.maven-surefire-plugin>3.5.5</version.maven-surefire-plugin>
        <build.time/>
    </properties>

    <dependencies>
        <!-- Serial communication -->
        <dependency>
            <groupId>com.fazecast</groupId>
            <artifactId>jSerialComm</artifactId>
            <version>${version.jSerialComm}</version>
        </dependency>

        <!-- CLI argument parsing -->
        <dependency>
            <groupId>commons-cli</groupId>
            <artifactId>commons-cli</artifactId>
            <version>${version.commons-cli}</version>
        </dependency>

        <!-- Sharp BASIC keyword registry and preprocessing -->
        <dependency>
            <groupId>ch.erzberger.sharpbasic</groupId>
            <artifactId>sharp-basic-core</artifactId>
            <version>${version.sharpbasic}</version>
        </dependency>

        <!-- Sharp BASIC ANTLR4 parser, detokenizer, and output visitors -->
        <dependency>
            <groupId>ch.erzberger.sharpbasic</groupId>
            <artifactId>sharp-basic-antlr</artifactId>
            <version>${version.sharpbasic}</version>
        </dependency>

        <!-- Lombok (compile-time only) -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>${version.lombok}</version>
            <scope>provided</scope>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${version.junit}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <!-- Fat JAR name without version number -->
        <finalName>SharpDataExchange</finalName>

        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <version>${version.maven-surefire-plugin}</version>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${version.maven-compiler-plugin}</version>
                <configuration>
                    <release>${java.version}</release>
                    <compilerArgs>
                        <!-- Required for Lombok on JDK 21, see https://inside.java/2023/07/29/quality-heads-up/ -->
                        <arg>-proc:full</arg>
                    </compilerArgs>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>deploy-local</id>
                        <phase>install</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <mkdir dir="bin"/>
                                <copy file="${project.build.directory}/${project.build.finalName}.jar"
                                      todir="bin"
                                      overwrite="true"/>
                                <echo>Installed to bin/${project.build.finalName}.jar</echo>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${version.maven-shade-plugin}</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <createDependencyReducedPom>false</createDependencyReducedPom>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>ch.erzberger.sharppc.exchange.SharpDataExchange</mainClass>
                                    <manifestEntries>
                                        <Implementation-Title>${project.name}</Implementation-Title>
                                        <Implementation-Version>${project.version}</Implementation-Version>
                                        <Build-Time>${build.time}</Build-Time>
                                        <!-- Suppress "restricted method" warnings from jSerialComm's
                                             System.loadLibrary() call on Java 22+ (JEP 472).
                                             The JVM reads this attribute from the main JAR manifest
                                             and applies enable-native-access=ALL-UNNAMED automatically. -->
                                        <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
                                    </manifestEntries>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
